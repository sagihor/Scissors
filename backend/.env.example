# Backend environment variables (copy to backend/.env and fill in)
PORT=3000

# AWS RDS MySQL connection
DB_HOST=
DB_PORT=3306
DB_NAME=scissors_db
DB_USER=
DB_PASSWORD=

# Google Gemini key (server-side only)
AI_API_KEY=

# Public URL of the deployed frontend (locks CORS in production)
FRONTEND_URL=