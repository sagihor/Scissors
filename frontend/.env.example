# Frontend dev server port (Parcel). No API URL needed:
# in production the backend serves the app on the same origin,
# and in local dev the app talks to http://localhost:3000 automatically.
PORT=5173